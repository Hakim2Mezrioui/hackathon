const initState = {
  perdus: []
};

const reducers = (state = initState, action) => {
  switch (action.type) {
    case "fetch-perdus":
    default:
      return state;
  }
};

export default reducers;
